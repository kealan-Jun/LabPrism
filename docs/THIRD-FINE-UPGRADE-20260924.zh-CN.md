# 第三人称精扫 v5m · 2026-09-24

本轮完成一次真实 GPU 训练、候选派生、实际 TensorRT 对照与完整 133 秒视频重算。选择升级第三人称精扫；粗扫候选增加纸张误报，继续使用 v2。训练与标注原件归 AnnotationWorkbench，部署状态以 NAS 回执为准。

## 数据与训练

新增原幅纠错 2 图、33 个已知目标、19 个未知区域，工作台 revision 2。使用部分监督，未知区域不当背景；20 张原复核训练图回放，2 张固定旧验证图保持。两张新图来自已有实验，属于训练反馈，不能算新场景独立验证。

YOLO26s 从现用第三人称 v2 继承，640 输入、实际 batch 16 / 尾批 6、BF16，30 轮 GPU 训练耗时 31.10 秒，峰值已分配显存约 4.87 GB；新优化器，不是完整恢复优化器。原 v5 在旧验证漏掉一个样品瓶，因此未上线。预先固定 0.5 比例合并父模型与 v5，得到 v5m；此步骤没有增加训练轮次。

## 同条件实际结果

比较真实 TensorRT 输出，置信度 0.25、同类 NMS IoU 0.7、匹配 IoU 0.5。新图排除未知区域中心内且不匹配已知对象的提议；完整场景准确率未测。

| 范围 | 原 v2（TP / FP / FN） | v5m（TP / FP / FN） | 用途 |
| --- | --- | --- | --- |
| 固定旧验证 2 图，精扫 640 | 23 / 1 / 4 | 23 / 1 / 4 | 小型旧能力回归 |
| 原训练回放 20 图 | 273 / 0 / 0 | 273 / 0 / 0 | 防止遗忘；不是验证集 |
| 新纠错训练 2 图 | 19 / 22 / 14 | 24 / 12 / 9 | 训练拟合反馈 |
| 固定旧验证 2 图，粗扫 416 | 17 / 1 / 10 | 17 / 2 / 10 | 粗扫候选拒绝 |

完整视频 667 个分析帧逐项保持原始 RGB、PTS、时间基和源身份，候选产生 20,318 个检测提议。检测阶段实测 8.76 秒，不是端到端服务速度或稳定吞吐。手姿、分割、OCR、动作本轮没有重算，不能借用旧版本输出充当新成绩。

短实看 10、40、80、130 秒及两张纠错训练帧：部分电脑/前臂误报减少，已知旧目标保持；仍漏第二只手、混淆玻璃容器和密集移液器。另有低分试管架过大框、130 秒重复烧杯框等局部回退。此次选择是范围有限、可回滚的精扫改善，未宣称误报消失或独立泛化达标。

## 模型和交付

- 推理 PT SHA256：`609ef1242bece88785e02c1a216a039e985138b59e60f6e2822814be61ce9e17`。
- 精扫 TensorRT SHA256：`87dd443772a77096633e3847a282499857abd894eadfa82ba01173b4fdef922b`，640 / FP16 / 静态 batch 4 / one2many。
- 只修改第三人称 PT 与精扫 engine 的配置映射；第一人称 v8、第三人称粗扫 v2 保持。
- 旧权重保留，配置原字节备份及独立 systemd drop-in 可回滚。采集与 Web 进程不重启。
- 自动训练 hold 与历史隔离数据不变。公开发布和 NPU 部署均未执行。

NAS 产品证据：`/mnt/realityloop-nas/LabPrism/evaluations/third-context-20260924/`。主要文件为 `iteration-receipt.json`、`candidate-video-acceptance.json`、`acceptance-erratum.json`、`engine-replay-and-fullframe-feedback/report.json` 与 `deployment-receipt.json`。80 秒观察原句过宽的修正独立保存，原验收摘要保持可审计。

原始标签与训练：AnnotationWorkbench NAS `datasets/labprism-source-expansion-20260923-v1/bulk-annotation-v1/` 下的 `third-fullframe-corrections-20260924`、`training-third-v5`、`third-v5-midpoint` 及四份 runtime-acceptance 对照。模型与代码留本地。

[完整视频检查](http://127.0.0.1:8031/inspection.html?clip=third-fine-v5m&t=50)。旧 v2 时序轮廓保留为历史对照，新精扫回放不混用旧掩膜。

## 剩余工作

完整产品目标继续有效。接下来利用现有分任务标签补足场景覆盖，迭代密集器材、遮挡手部与分割；同时维持实际使用模型与页面证据一致。动作步骤、OCR 可靠绑定、跨视角身份/精确同步、全天稳定性与新实验泛化仍不能据此次检测升级宣布完成。
