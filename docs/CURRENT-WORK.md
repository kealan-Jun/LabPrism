# 当前工作 · 2026-09-24

2026-09-24：第一人称精扫v11完成30轮GPU训练并已由生产PID3239200实际加载，开发集176TP/22FP→178TP/21FP，各类TP保持或增加。101秒/506帧检测、CUDA手姿和SAM2完整重算已接回放；旧粗扫和第三人称保持，旧退出180秒超时仍在。旧PT/引擎输入填充不一致已更正，详见[训练、实际升级及取舍](REPLAY-PRESERVATION-UPGRADE-20260924.zh-CN.md)。

2026-09-24：新视频补充2图19实例后完成30轮GPU纠错训练，107train/16固定val、实际batch16，64.63秒。新训练帧11TP/3FP→17TP/0FP，但固定验证176TP/22FP→170TP/21FP；两个保守混合候选也回退，保留线上v8。手姿新增4已知点，本轮未训练。见[纠错训练与实际决定](FRESH-CORRECTIONS-20260924.zh-CN.md)。

2026-09-24：新视频已能从原片回执直接调用现用TensorRT检测，串接CUDA手姿与上游SAM2。两段实际新输入共582帧，667组手姿、4,106个时序掩膜观察，已接本地检查页；握拳、瓶盖/瓶身及袖口误报仍在。117+23项检查通过，生产模型保持；Web队列完整串接尚未完成。见[新视频实际链路](FRESH-VIDEO-PIPELINE-20260924.zh-CN.md)。

2026-09-24：VisionCortex日回执已改为逐条投影及流式JSON，91a210c已切换至PID2329113。两份真实回执字节/摘要保持，隔离进程峰值273→99MiB、189→110MiB，CPU耗时增加；376项通过。模型及采集/Web保持，整机长期OOM消除仍未证明。见[实测与生产部署](RECEIPT-STREAMING-20260924.zh-CN.md)。

2026-09-24：分析进程发生主机OOM后，VisionCortex已修复推理失败遗留解码线程/子进程，双仓同步6d5c84b并切换至PID2142382。209项通过/4跳过，320真实帧输出逐字节一致，真实故障注入的两个解码器均回收。采集/Web保持；长期OOM消除未证明。见[本轮修复与实证](SCAN-CANCELLATION-20260924.zh-CN.md)。

2026-09-24：手姿训练覆盖扩至287训练图（275张初标）/4,568候选点，12轮GPU实际batch32、16.36秒完成。相同GT手框/四向阈值已标点135/159→152/159，固定验证13/15持平；761帧重算并接新旧检查页，握拳/重叠骨架仍错。未更换生产手姿，连续分析仍由VC承接；见[扩充训练与边界](POSE-EXPANSION-20260924.zh-CN.md)。

2026-09-24：当前第一人称v8已驱动761帧CUDA手姿重算，输出311→1,063组但握拳/遮挡关节仍错；第三人称v5m已重算667帧/5,336个官方SAM2时序观察。新增版本化手姿来源交接，未执行跟踪时不生成事件结论。新旧回放均保留；本轮无生产权重/服务变更，见[当前检测的后续推理](CURRENT-PIPELINE-20260924.zh-CN.md)。

2026-09-24：分割训练覆盖扩至4,172图/50,074实例，10轮实际batch16、480.13秒完成，每轮使用全部训练图。原图小开发IoU v3 0.73009→v6 0.73449，实际SAM 0.86266；移液器及已标语义像素回退，候选不晋级。88.5%图来自同一天，新增候选不是新增复核真值。生产模型/服务保持，见[覆盖训练与决定](SEGMENTATION-COVERAGE-20260924.zh-CN.md)。

2026-09-24：VisionCortex 扫描运行时 49977f8 已部署至新 worker 1223974，模型保持第一人称 v8、第三人称精扫 v5m/粗扫 v2。实际320帧新旧输出逐字节一致，观测队列积压19→5；无关角色换模缓存复用通过。181项检查通过、双仓同步完成；新进程已实际加载v5m，观察到34帧第三人称精扫、569帧第一人称精扫。总RSS改善及长期OOM修复未证明，旧进程退出仍触及180秒超时；采集/Web保持。见[运行时升级与边界](SCAN-RUNTIME-UPGRADE-20260924.zh-CN.md)。

用户授权本对话负责完整官网、推理系统及 GitHub 接入，并保留 AnnotationWorkbench 的数据/标注/训练归属。沿用 9/17–11/11 的八周计划；首个可复现真实演示已交付；本轮推进模型候选比较与可核查质量证据。

## 最新优先级 · 用户已明确

2026-09-18：专注模型能力与真实效果。优先戴手套手姿、背景误检、跟踪/遮挡恢复与语义分割；NPU 转换、量化和实板部署全部后置，当前不索取板卡信息。官网继续作为真实结果验证入口。

## 2026-09-18 continuous / temporal 交付

- 已从 AnnotationWorkbench 接收五份带来源回执的完整提供文件，四份第一轮连续候选已完成 5 Hz 全文件重解码；另完成主动第三人称视角。素材仍属于 train 开发组，原件标记为既有实验剪辑，不能宣称三个不同实验的未剪辑完整记录。
- 连续候选实际运行 RTMPose 2D 手姿、相机全局运动补偿与外观关联、CPU RapidOCR 和二维指尖接近观察。首四段合计 2,575 帧；OCR 228/139/52/7 条文字候选，所有准确率、IDF1、事件准确率保持 null。接近区间只是几何候选，不是物理接触、动作或实验步骤。
- SAM2 视频记忆候选已在称量片段 646 帧、13 个窗口实际运行；每窗最多三个检测框提示，窗口间身份重置。退化轮廓已过滤并通过 v3 合同；时序质量保持 null。查看器新增独立时序掩码层和状态说明。
- 实际图像抽查保存在运行根 `evaluations/continuous-review-20260918-v1/`：称量、溶解、清洗均发现手姿点位偏移；原第三人称侧视样例为离心机背景、无主动手部，因此保留为背景诊断并接入主动第三人称片段。抽查是项目代理检查，不是真值。
- 官网 12 组本地片段已重建，Chrome 四种宽度、导航、失败切换清理、图层、OCR 多边形、事件定位、长片段定位和下载均通过，回执为 `evaluations/browser-continuous-probe-20260918-v11/receipt.json`。预览仅监听 `127.0.0.1:8031`。
- 合同文档新增 `packages/contracts/video-result-v3.md`；全量 Python 测试 69 项通过。连续/时序阶段的父级耗时现在仅放在 `baseline_metrics`/`parent_metrics`，候选指标不再复制父级整链路耗时。

### 仍未完成的验收边界

没有新标注、训练、独立来源质量集、完整实验动作/步骤真值、OCR 读数准确率、遮挡身份真值、标定全局几何、NPU 转换或实板执行。AnnotationWorkbench 的 `all_workbench_training` 质量保持仍有效；不移除该保持，也不晋级任何候选为生产模型。现有证据支持可复现研究演示，不支持“全部能力已验收”或超越参考系统的结论。

## 本轮模型推进 · 接在原诊断包之后

- 三段候选各 80 帧，`runs/candidate-20260918-v2`：RTMPose 二维手姿、因果身份关联、COCO 类别冲突、SegFormer 真正语义输出均已运行。手套第一/第三人称有输出帧 80/80、62/80，裸手 80/80；不是关节准确率。低分点不画、丢检不补画，原基线与未通过候选全部留存。
- 语义候选把实验台误分为墙/服饰等，不通过质量评审；网页默认关闭并明确标识。轨迹仍碎片化，身份与遮挡恢复未验收。
- AnnotationWorkbench 已冻结 97 图项目复核导出，仅取 18 图 val 做检测内部回归。16 张第一人称已被基线接触，2 张第三人称是裁剪；未打开封存测试，不能宣称独立泛化。基线 P/R 为 88.07%/72.43%、100%/55.56%。
- 笔记本冲突自动过滤误删 2 个真实天平；1280 输入分辨率两个视角均退化。均不晋级，v2 保留原检测、仅提示冲突。实际看图范围及失败决策见运行根 `evaluations/candidate-review-20260918-v2/review.json`。
- 查看器已接六组候选/基线，增加轨迹、默认关闭的语义检查、冲突说明、二维空深度与分数过滤。真实 Chrome 六组交互和四种宽度通过；共 480 个候选/基线帧通过 PTS/RGB 身份核验。61 项测试与项目检查通过。
- 训练仍受生产方现有 all_workbench_training 质量暂停约束；本轮没有新标注或训练，没有商业/生产模型晋级。NPU 按用户要求全部后置。

本轮代码已推送 GitHub `codex/bootstrap`，实现提交 `82ffd4b`。NAS 研究包 `releases/model-candidates-20260918-v1` 共 452 文件回读通过，manifest SHA256 `532502cf55e5fc72172cf20635f6dbd9236a5cb2b2af5919586c08f464ecfc2f`。从包内源码解出的无 Git 独立目录，使用包内权重与父预测复跑三段新增阶段；240/240 帧的手姿、冲突、轨迹、语义图 hash 与所有候选预测字段一致。补充复跑证据 267 文件保存在 NAS `evaluations/model-candidates-20260918-v1-replay`，回执 `receipts/model-candidates-20260918-v1-replay.json`。这是研究证据完整性与候选阶段复现，不是新训练、完整流水线性能、质量晋级或生产部署。

详细结果、阈值、许可、测量范围和复现命令见 [模型候选比较](MODEL-CANDIDATES.zh-CN.md)。后续直接从独立来源验证覆盖、戴手套关节与语义标注、检测背景混淆和追踪碎片化继续，不重复启动预览服务。

## 原诊断包交接记录（以下为此前已完成工作）

## 接手核实

- 本地分支 `codex/bootstrap`，接手时 HEAD `c2ea7bf`；origin 正确，远端当时为 `597ef82`，本地已有两个未推送实现提交。保留并继续了三个修改文件和未跟踪的证据渲染脚本。
- 旧文档落后于代码：实际上已有三段基线输出和查看器。接手时 8031 无监听；现由 `labprism-preview.service` 提供 `http://127.0.0.1:8031/demo.html`，只监听回环。
- 检查进程工作目录与本地终端任务队列，没有发现其他正在操作 LabPrism 的终端任务；另一终端位于 FieldRecognition。本轮未创建任务或代理，未改动其他项目服务。
- RTX 3090 Ti 24 GB 与已有工作负载共享。检测/SAM 使用 CUDA，MediaPipe 使用 CPU/XNNPACK；未停止已有 GPU 工作负载。
- NAS 为真实 CIFS 挂载，卷 ID `b01a5b29-90f0-4bf4-b060-416f4fc67da0` 与项目身份一致。

## 已有证据与本轮结果

1. **素材与模型接收**：沿用 AnnotationWorkbench 的版本化素材包和模型回执。两段溶解搅拌素材来自同实验第一/第三人称；第三段为 NAS 裸手操作电脑对照。三段均为 train 开发组、约 8 秒，原模型接触状态 unknown。数据集所有权没有转移；第三段不算完整实验。
2. **真实推理**：检测候选 + SAM 2.1 Tiny 框提示逐帧实例分割 + MediaPipe 21 点已运行。原版本在 `runs/baseline-20260917-v2`。本轮 `runs/reproduction-20260918-v2` 重跑三段，每段 80 帧，240 帧预测载荷逐帧相同；两组共 480 个解码帧的 PTS/RGB hash 通过校验。
3. **真实缺口**：两段戴手套视频手姿输出均为 0/80 帧；NAS 裸手对照为 80/80 帧有输出，但屏幕误检为天平。实际查看三个约 4 秒时刻的原画/预测对照和浏览器截图，记录为项目代理抽样检查，没有创建人工真值或完整逐帧标签。
4. **产品**：三页官网、片段切换、播放/暂停/倍速、逐分析帧、原画开关、框/mask/骨架/标签开关、实例详情、定位点及 JSON 下载已真实浏览器验证。320/390/768/1440 宽度无横向溢出。事件、OCR 和轨迹未生成。
5. **修复**：提前缓存 PyAV 容器时长，消除关闭容器后读取失效原生元数据导致的复跑失败；切换片段时清空旧证据和下载链接，失败时禁用操作，成功载入后保留倍速；预览校验完整输入/模型/证据回执。
6. **内部交付工具**：NAS 发布校验卷身份、模型/素材/运行 SHA256 与干净源码，打包完整输入、权重、源码、网页和评审证据；提供可迁移路径的复跑入口。归档源码无 Git 时保留发布包提交与 hash，不借用启动目录的 Git。READY 仅代表文件完整性，不代表算法晋级。

## 验证入口

全部运行证据位于 `/home/x1/.local/share/labprism`，不进 Git：

- `receipts/takeover-20260918.json`：接手核实。
- `receipts/producer-demo-delivery.json` 与 `receipts/baseline-models-20260917.json`：生产方交接；原训练记录仍在 AnnotationWorkbench。
- `receipts/reproduction-verification-20260918-v2.json`：逐帧源图校验及复跑比较。
- `evaluations/browser-20260918-v2/receipt.json`：真实 Chrome 检查及截图。
- `evaluations/review-20260918-v1/review.json`：错误样例和代理检查身份。
- `logs/reproduction-20260918-v1.log`：保留修复前失败；`logs/reproduction-20260918-v2.log` 为修复后运行。

39 项单元测试与 `python3 scripts/check_project.py` 已通过。三段复跑离线吞吐分别为 6.142 / 7.450 / 8.915 分析帧/s；输入以 10 Hz 抽样。它们是共享 GPU 下的本次壁钟测量，不能作全帧实时速度或相对旧版性能提升结论。

GitHub 已完成普通提交与推送，远端分支为 `codex/bootstrap`；内部包源码提交 `c14baf4`。工作媒体、数据、预测、权重与日志未进入 Git。

内部 NAS 交付：`/mnt/realityloop-nas/LabPrism/releases/diagnostic-demo-20260918-v2`，113 个文件回读通过，`release.json` SHA256 为 `4d4b432ae99cd864b4efe370bb98f6ad285c4342c8a51cb2df11cae0118bd9c0`。v1 保留，没有覆盖或公网发布。

从 v2 包解出的源码在无 `.git` 独立目录中，直接使用包内素材/权重成功复跑第一人称片段；80/80 帧源图校验与预测载荷比较通过。回执为运行根 `receipts/package-replay-verification-20260918-v2.json`。补充的三段复跑与包复跑共 40 个文件已归档到 NAS `evaluations/diagnostic-demo-20260918-v2`，逐文件 hash 见 NAS `receipts/diagnostic-demo-20260918-v2-acceptance.json`。这是内部离线交付与完整性验收，没有模型质量晋级。

## 下一轮直接继续

1. 在 AnnotationWorkbench 依据失败样例交付更多真实实验开发视频，优先 NAS 中有效的第一/第三人称操作、戴手套/透明/密集/新背景；固定验证分组与封存测试边界。现有三段不满足 W4 三个不同实验完整视频要求。
2. 对同一开发输入比较戴手套手姿改进候选、检测误检控制与独立语义分割基线。需要补标签或训练时在 AnnotationWorkbench 实施，保留质量隔离和父模型记录。
3. 实现并评测器材/手部身份关联与遮挡恢复，再接视频分割、OCR、交互和步骤时间线；未产生的输出继续明确为空。
4. 完善官网能力状态、长视频及键盘体验。正式品牌/联系资料、公开素材及 NPU 板卡未明确时，相关发布/实板验收保持未完成。

**完成边界**：代码、真实推理和本地浏览器链路已验证；本轮无实际标注或训练。准确率、泛化、时序质量、量化损失、实板 NPU 均未测得，未宣称超过参考系统。v0.1 和 v1.0 尚未验收。
