# 日回执内存优化与生产部署

为减少连续分析的内存峰值，VisionCortex `91a210ce13e5b279aa0109b8c28fa9f6d9c3ba47` 已将日回执投影改为逐条验证、暂存、事务更新和读取，并流式计算摘要、写入索引 JSON。完整审计内容、时间、来源、五目录结构和原子发布规则保留。

两份已有生产回执共 21,456,862 字符，在隔离进程对照：

| 工作 | 旧版峰值 RSS | 新版峰值 RSS | 旧版耗时 | 新版耗时 |
| --- | ---: | ---: | ---: | ---: |
| 读取、摘要、序列化 | 273.40 MiB | 98.84 MiB | 1.029 s | 2.612 s |
| 全量及增量回执投影 | 188.91 MiB | 109.85 MiB | 1.339 s | 2.451 s |

独立进程峰值分别下降约 **64% / 42%**，CPU 耗时增加。46,014,038 字节输出逐字节一致，两个投影记录的完整摘要及增量结果一致；没有通过删掉诊断字段降低内存。这是回执处理范围的实测，不能直接推导整机内存改善或全链路速度。

376 项相关测试通过，包括读取中断、坏 JSON、SQL 中途失败、fsync 失败和旧回执保留；变更文件 Ruff 与编译检查通过。全仓 Ruff 的原有两个采集文件问题未修改。两个 VisionCortex GitHub 仓库的 main 和共享分支均核对为 `91a210c`。

生产只切换分析代码，模型仍为第一人称 v8、第三人称精扫 v5m / 粗扫 v2；采集、Web、GPU 配额及生产方质量保持不变。原始测量、完整性证明、运行时部署和回滚回执存于：

`/mnt/realityloop-nas/LabPrism/evaluations/receipt-streaming-20260924-v1/`

`runtime-deployment.json` 记录切换，`live-verification.json` 记录新进程实际代码、模型加载和生产任务进展。整机长期 OOM 是否完全消除仍未证明；原始对象和单个超大字符串仍占内存。强制终止可能遗留临时缓存，不能将其视为有效回执。本轮新增训练和标签均为 0。

08:12 CST 实际核验：新进程 PID 2329113 运行干净的 `91a210c`，日志已加载全部四个角色/阶段引擎；任务快照包含第一人称精扫 493 / 145 帧、第三人称精扫 239 / 37 / 94 帧。采集与 Web PID、模型配置未变，自动重启次数为 0。进程当时 RSS 6.36 GiB、启动以来高水位 10.77 GiB、交换为 0；该短窗口不能证明长期稳定。旧进程退出仍耗时 180.84 秒，本轮没有解决退出超时。

完整产品目标继续。后续直接复用现有新视频来源回执、TensorRT 检测、CUDA 手姿、上游 SAM2 和 FieldRecognition 输出，把分散推理阶段接入 VisionCortex 的实际视频任务；真实纠错与训练继续按各任务效果推进。
