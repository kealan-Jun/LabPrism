# 模型迭代与服务升级 · 2026-09-23

目标是完整可运行产品和实际模型能力。用户已授权连续训练、验证后及时替换服务模型；不再等待每次单独授权。数据/标签/日志/报告在 NAS，代码和模型在本地。本文是本轮证据汇总，不表示整个产品完成。

## 数据与标注

11:41 UTC 的工作台快照：有效候选 **18,136** 张。原始库存、重复素材和无意义图片不计入这个数字。

| 任务 | 实际当前数量 | 状态 |
|---|---:|---|
| 检测初标 |18,136图|模型提议及公开源标签，不是全部已复核真值|
| 实例分割初标 |13,478图 / 219,427实例|SAM框提示生成的草稿|
| 部分语义分割初标 |13,478图 / 69,608区域|只覆盖已标像素，其余为ignore；不是全图材料/液体语义真值|
| 手姿初标 |7,294图 / 10,301手|114,670提议点，101,651未知点|
| 有实际项目复核的图片 |32图|不冒充独立人工真值|
| 本轮检测训练与开发导出 |1,513张唯一图|五次训练复用同批，不能相加成五倍|

第一人称使用354张高置信伪标签与97张已复核训练图，固定16张开发验证；第三人称1,024张伪标签与20张已复核训练图，固定2张开发验证。伪标签使用已有部分正例监督损失，未标区域不直接当背景；已复核回放提供完整监督。角色未知的公开25类数据没有强行塞进23类模型。历史来源分组/近重复冲突素材暂未纳入这一批，隔离的旧问题批次没有解除。

## 本轮五次已完成训练

均在3090 Ti上实际执行，FP16 AMP、实际batch16。继承已批准角色检查点，708/708网络张量加载；重新建优化器，不称完整优化器恢复。

| 运行 | 输入 / 轮数 | 父模型→候选 mAP50–95 | 选择 |
|---|---|---|---|
| v1第一人称 |640 / 5|0.68216→0.72255|精确率下降，继续迭代|
| v1第三人称 |640 / 5|0.74966→0.72618|回退，继续迭代|
| v2第一人称 |640 / 15|0.68216→0.74434|固定验证改善，真实视频前臂/天平误报，暂缓该版本|
| v2第三人称 |640 / 15|0.74966→0.76554|合格受控升级，保留回滚|
| v3第一人称 |960 / 15|0.75163→0.76923|实际640服务条件下进一步比较；同一误报原图未再出现该天平框|

v2/v3复用VisionCortex按来源平衡采样，避免大量相邻伪标签淹没已有复核数据。训练结果归AnnotationWorkbench；消费端部署决定另留回执。

## 与真正服务模型比较

实际服务此前是21类YOLO TensorRT one2one、640输入、confidence0.25/IoU0.55。训练父模型和当前服务模型并非同一权重，因此不能只看训练内增量就替换。

四个v2 FP16引擎已经实际构建：精扫640/batch4、粗扫416/batch16，one2many分支。新引擎配合现有按角色去重，在固定开发集的结果如下：

| 视角 | 旧服务TP / FP / FN | v2候选TP / FP / FN |
|---|---|---|
| 第一人称16图 |134 / 28 / 80|172 / 12 / 42|
| 第三人称2图 |18 / 4 / 9|23 / 1 / 4|

另外两段真实NAS视频共400配对帧的PTS和解码像素摘要逐一相同。第三人称密集试管盖检出明显增加，静态台面接受动作仍为0；粗扫另40帧同像素比较通过。第一人称v2出现前臂大框被误判为天平，因此不因总分提高直接部署它。两段视频不是同步实验，且有历史近重复暴露关系，不能称独立最终测试。

## 部署状态与边界

第三人称已提交版本化配置及必要的分析服务重载，新分析进程已运行，正在核验真实模型加载；第一人称v3的两套引擎也已完成；实际引擎开发对照134TP/28FP→163TP/13FP，但真实片段的烧杯覆盖仍不足，保留候选继续修正。最终实际状态以NAS `bulk-annotation-v1/deployment-third-v2/receipt.json` 为准。采集和网页未停止。旧模型文件、原配置与回滚信息保留。旧进程未能在原有15分钟期限内完全退出，由systemd按既有超时策略结束并启动新进程；退出耗时问题需后续修复。

VisionCortex上游新增显式按角色类别表，允许编号兼容的追加类别，不允许错位映射；87项相关检查通过，双GitHub已同步de8e4c65d94b468917a7941e9cf8a09f1c1c6d84。模型可以按视角独立升级。生产语义/动作门禁保持；隔离比较禁用的模块不会被带进生产配置。

仍需继续：扩大真实独立验证覆盖；减少前臂、反光和器材相似类误报；补足完整手姿/遮挡和分割质量；继续训练可证明改善的任务模型；将最佳版本接到实际服务及官网真实结果。模型计数、网页完成度、独立泛化、全天多视角覆盖和全产品验收分别报告。

原始证据根：`/mnt/realityloop-nas/AnnotationWorkbench/datasets/labprism-source-expansion-20260923-v1/bulk-annotation-v1/`。关键目录：`training-v1`、`training-v2`、`training-v3`、`runtime-acceptance-v2`、`runtime-video-v2`、`runtime-export-first-v3`、`deployment-third-v2`。

## 20:31 CST 运行完整性修复

第三人称新粗扫和精扫引擎已在生产进程实际加载。共享GPU时发现Ultralytics框去重时限告警，存在整批后续帧被静默清空的风险。已复用安装好的torchvision CUDA NMS，并增加按推理线程捕获超时、整批最多三次执行、仍超时则拒绝提交不完整帧的处理；不全局修改第三方库。两段共80真实帧在修复前有效输出和修复后逐字节一致；65项检测/本体检查及18项共享推理检查通过。双仓main及工作分支同为f84974f8838ba8f2ddbecb2522c21f5c3c8af167。修复正在服务重载，原模型和回滚保持；退出等待显式限制为180秒，未完成任务由既有持久检查点恢复。不能把带超时告警的早期运行当作完整可靠验收。

20:38 CST：f84974f生产进程已实际加载新的第三人称粗扫引擎，当前新进程日志没有NMS超时告警；修复后的精扫加载仍待实际任务记录。分析、采集、网页与共享标注均active。完整长期运行验收仍未完成。


## 21:18 CST 扩大训练批次与启动性能修复

训练/验证导出由1,513张扩为 **13,135张唯一像素图**，不是新增加13,135张，也不是全部人工真值。第一人称4,175 train（4,078伪标签＋97复核回放）/16 val；第三人称8,942 train（8,922伪标签＋20复核回放）/2 val。全部媒体与标签在NAS，模型/代码在本地。角色未知的ChemEq25未被强行赋予视角。

新增显式开发素材复用协议：历史OCR/显示窗开发用途导致09-15至09-22关联组件被过度排除，现在作为新检测训练用途明确登记。现有两个角色验证集的版本、媒体和标签逐项保持；test/holdout不增加伪标签，隔离旧批次不解除。历史用途不重写；这些组件及共享模块的旧开发评测不再称独立。分区仍按完整关联组件隔离，训练平衡采样使用组件内原始采集日，不能用采样分层拆分验证。既有批准回放本身含历史train/holdout混合暴露，仅保留原始证据，不称独立。

第一人称已实际进行640、15轮、FP16、batch16训练；第三人称训练请求从已部署v2权重继续，不回退到更旧父权重。第三人称请求完成不代表已经训练。所有候选仍需同条件比较；生产当前第一人称保持旧模型、第三人称使用已部署v2。

发现并修复训练启动标签映射的平方级遍历，改为一次索引；文件哈希改为8路有界并行，字节/版本/隔离验证不减少。实测第一人称4,191张的完整校验11.27秒、映射3.99秒。此前300秒GPU等待参数被程序拒绝，以及旧启动扫描被主动停止，均没有完成训练并保留失败记录。

现有手框的7,298张手姿提议已覆盖；重复空跑的原生模型进程退出曾SIGSEGV，现先核验已有提议与源/检查点身份，无新提示则不加载GPU模型。共享分割已恢复并增量完成，随后为实际检测训练主动让出自身GPU；采集、生产分析和Web保持。网页实查原有区域外复核图仍为8框/1ignore/3个项目复核mask，未被草稿覆盖。大NAS项目列表初次加载仍慢，未把此项记为已优化。

数据仍明显不均衡：第一人称管盖34,769实例、样品瓶5,285，烧杯88、蓝盖瓶37、单支枪头1；第三人称烧杯3,272，但开口试剂瓶只有1。以上包含伪标签实例，数量不能代替准确性。下一轮应按真实错误与薄弱类别取样、修正，避免只增加大量同类静态画面。

证据：NAS `training-v4b/{dataset-summary,source-receipt,browser-check}.json`、两角色实际请求/日志、`development-reuse-v2.json`。相关28项测试通过；源代码快照保存在AnnotationWorkbench本地运行目录，NAS只记录其哈希回执。


第一人称v4实际完成15轮：mAP50–95 0.682155→0.721312，P0.836409→0.877401，R0.769655→0.791609；本次训练与验证592.048秒，峰值4136.61MiB。实际旧TensorRT与新PyTorch候选在640、相同开发图并应用服务去重后为134TP/28FP/80FN→174TP/28FP/40FN；这还不是v4引擎实测。天平出现4个新误报（旧服务0），且整体FP高于此前v2引擎的12，因此本版保留候选、不部署，也不为它重复构建引擎。模型变新和数据变多不自动等于胜出。

第三人称v4已从实际部署v2检查点启动GPU训练，使用8,942训练图。自有标注批处理暂让出GPU，已有完成/失败/超时后恢复该标注服务的独立进程保障；不涉及暂停生产分析。现有两个角色服务模型与回滚保持。


## 22:00 CST 困难样本修正与数值恢复

第一人称v5b已完成10轮CUDA FP16/batch16训练，保留451张原训练回放并加入3张有来源的实际复核裁剪（4个器材实例、1个纯前臂负例），16张验证保持。训练及框架验证49.680秒，GPU峰值4195.22MiB。父模型v2到候选mAP50–95为0.744337→0.743866，P0.876294→0.908376，R0.816712→0.792366。固定开发集与旧服务比较并应用原去重后134TP/28FP/80FN→173TP/13FP/41FN；新候选是PyTorch，旧服务是TensorRT，不混称同后端精度。实际25/45/65秒原幅检查仍有前臂天平/手框误报和烧杯漏检，继续保留候选，不部署。小裁剪拟合不能代替完整场景改善。

已经补齐45/65秒两张完整原幅的区域外复核：4个已知器材框，未逐件复核区域明确ignore，45秒额外保留前臂区域的负监督。此批需使用既有部分监督适配器，尚未导出或训练。两次含玻璃边缘的负裁剪均排除保留，不把真实物体当背景。来源已经参与开发及训练选择，不能称独立测试。

第三人称v4在5个完整epoch后、第6轮中出现FP16非有限损失并被中止。BF16的亚像素检测一致性检查与随后真实监督损失检查均未通过；没有放宽容差或使用失败候选。检查点的EMA与优化器张量已逐项确认有限，但现有来源平衡采样器明确不支持optimizer resume，因此不移除该保护。v4c使用该有限第5轮权重明确重新初始化优化器，在同一8,942训练/2验证数据上执行10轮CUDA FP32，当前实际进入第1轮、batch16、GPU约7.6GiB。它是权重续训加新优化器，不能称完整优化器恢复，也未完成或部署。

先前恢复脚本因临时systemd标注unit在停止后被回收而失败。已改为持久的labprism-shared-annotation.service，由每个训练unit的ExecStopPost在完成/失败/超时后启动，实际已验证恢复成功。只调度自有标注，不暂停采集/Web/生产分析。分割提议累计15,621张、305,092实例；手姿提议覆盖7,298张已有手框图，不等于所有可见手或已复核真值。

截至本次记录，7个完整检测训练运行完成；中断训练的5轮及AMP检查失败另记，不增加完整训练次数。新增裁剪不增加独立来源。权威记录在NAS training-v5b、first-hardcases-v5、training-v4b、training-v4c；代码和检查点仍在本地。目标仍是完整产品和模型：后续对原幅混淆继续定向训练，并仅将验证胜出的候选交付实际服务。
