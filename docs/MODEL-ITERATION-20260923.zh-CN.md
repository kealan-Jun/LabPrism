# 模型迭代与服务升级 · 2026-09-23

目标是完整可运行产品和实际模型能力。用户已授权连续训练、验证后及时替换服务模型；不再等待每次单独授权。数据/标签/日志/报告在 NAS，代码和模型在本地。本文是本轮证据汇总，不表示整个产品完成。

## 数据与标注

11:41 UTC 的工作台快照：有效候选 **18,136** 张。原始库存、重复素材和无意义图片不计入这个数字。

| 任务 | 实际当前数量 | 状态 |
|---|---:|---|
| 检测初标 |18,136图|模型提议及公开源标签，不是全部已复核真值|
| 实例分割初标 |13,478图 / 219,427实例|SAM框提示生成的草稿|
| 部分语义分割初标 |13,478图 / 69,608区域|只覆盖已标像素，其余为ignore；不是全图材料/液体语义真值|
| 手姿初标 |7,294图 / 10,301手|114,670提议点，101,651未知点|
| 有实际项目复核的图片 |32图|不冒充独立人工真值|
| 本轮检测训练与开发导出 |1,513张唯一图|五次训练复用同批，不能相加成五倍|

第一人称使用354张高置信伪标签与97张已复核训练图，固定16张开发验证；第三人称1,024张伪标签与20张已复核训练图，固定2张开发验证。伪标签使用已有部分正例监督损失，未标区域不直接当背景；已复核回放提供完整监督。角色未知的公开25类数据没有强行塞进23类模型。历史来源分组/近重复冲突素材暂未纳入这一批，隔离的旧问题批次没有解除。

## 本轮五次已完成训练

均在3090 Ti上实际执行，FP16 AMP、实际batch16。继承已批准角色检查点，708/708网络张量加载；重新建优化器，不称完整优化器恢复。

| 运行 | 输入 / 轮数 | 父模型→候选 mAP50–95 | 选择 |
|---|---|---|---|
| v1第一人称 |640 / 5|0.68216→0.72255|精确率下降，继续迭代|
| v1第三人称 |640 / 5|0.74966→0.72618|回退，继续迭代|
| v2第一人称 |640 / 15|0.68216→0.74434|固定验证改善，真实视频前臂/天平误报，暂缓该版本|
| v2第三人称 |640 / 15|0.74966→0.76554|合格受控升级，保留回滚|
| v3第一人称 |960 / 15|0.75163→0.76923|实际640服务条件下进一步比较；同一误报原图未再出现该天平框|

v2/v3复用VisionCortex按来源平衡采样，避免大量相邻伪标签淹没已有复核数据。训练结果归AnnotationWorkbench；消费端部署决定另留回执。

## 与真正服务模型比较

实际服务此前是21类YOLO TensorRT one2one、640输入、confidence0.25/IoU0.55。训练父模型和当前服务模型并非同一权重，因此不能只看训练内增量就替换。

四个v2 FP16引擎已经实际构建：精扫640/batch4、粗扫416/batch16，one2many分支。新引擎配合现有按角色去重，在固定开发集的结果如下：

| 视角 | 旧服务TP / FP / FN | v2候选TP / FP / FN |
|---|---|---|
| 第一人称16图 |134 / 28 / 80|172 / 12 / 42|
| 第三人称2图 |18 / 4 / 9|23 / 1 / 4|

另外两段真实NAS视频共400配对帧的PTS和解码像素摘要逐一相同。第三人称密集试管盖检出明显增加，静态台面接受动作仍为0；粗扫另40帧同像素比较通过。第一人称v2出现前臂大框被误判为天平，因此不因总分提高直接部署它。两段视频不是同步实验，且有历史近重复暴露关系，不能称独立最终测试。

## 部署状态与边界

第三人称已提交版本化配置及必要的分析服务重载，新分析进程已运行，正在核验真实模型加载；第一人称v3的两套引擎也已完成；实际引擎开发对照134TP/28FP→163TP/13FP，但真实片段的烧杯覆盖仍不足，保留候选继续修正。最终实际状态以NAS `bulk-annotation-v1/deployment-third-v2/receipt.json` 为准。采集和网页未停止。旧模型文件、原配置与回滚信息保留。旧进程未能在原有15分钟期限内完全退出，由systemd按既有超时策略结束并启动新进程；退出耗时问题需后续修复。

VisionCortex上游新增显式按角色类别表，允许编号兼容的追加类别，不允许错位映射；87项相关检查通过，双GitHub已同步de8e4c65d94b468917a7941e9cf8a09f1c1c6d84。模型可以按视角独立升级。生产语义/动作门禁保持；隔离比较禁用的模块不会被带进生产配置。

仍需继续：扩大真实独立验证覆盖；减少前臂、反光和器材相似类误报；补足完整手姿/遮挡和分割质量；继续训练可证明改善的任务模型；将最佳版本接到实际服务及官网真实结果。模型计数、网页完成度、独立泛化、全天多视角覆盖和全产品验收分别报告。

原始证据根：`/mnt/realityloop-nas/AnnotationWorkbench/datasets/labprism-source-expansion-20260923-v1/bulk-annotation-v1/`。关键目录：`training-v1`、`training-v2`、`training-v3`、`runtime-acceptance-v2`、`runtime-video-v2`、`runtime-export-first-v3`、`deployment-third-v2`。

## 20:31 CST 运行完整性修复

第三人称新粗扫和精扫引擎已在生产进程实际加载。共享GPU时发现Ultralytics框去重时限告警，存在整批后续帧被静默清空的风险。已复用安装好的torchvision CUDA NMS，并增加按推理线程捕获超时、整批最多三次执行、仍超时则拒绝提交不完整帧的处理；不全局修改第三方库。两段共80真实帧在修复前有效输出和修复后逐字节一致；65项检测/本体检查及18项共享推理检查通过。双仓main及工作分支同为f84974f8838ba8f2ddbecb2522c21f5c3c8af167。修复正在服务重载，原模型和回滚保持；退出等待显式限制为180秒，未完成任务由既有持久检查点恢复。不能把带超时告警的早期运行当作完整可靠验收。
