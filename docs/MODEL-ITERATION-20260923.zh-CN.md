# 模型迭代与服务升级 · 2026-09-23

更新：23:27 CST。目标是完整产品及持续改善的真实模型。用户已授权训练、同条件验证后及时更新服务；不以标注完成或训练请求作为交付。

## 已上线的实际版本

第一人称 **v8**、第三人称 **v2** 均已由生产分析进程实际加载，包含粗扫416/batch16和精扫640/batch4两套TensorRT FP16引擎。第一人称权重SHA256为 `70b10e18c076332546ca139616e274799f12108b78c56341a0abd6e3c9a9ec9c`。生产代码 `f84974f8838ba8f2ddbecb2522c21f5c3c8af167`，进程2744207于15:01:27 UTC启动；加载日志及运行心跳已核验，非仅修改配置。旧权重、旧配置与按角色回滚保留。

## 评测更正与部署依据

**此前比较脚本仅对候选应用最终去重，旧模型却沿用原始计数，导致不公平比较。已修复并增加回归测试。此前“第一人称误报28→13”的结论撤回。** 两边在相同输入、阈值、最终去重和标签上重算，得到：

| 实际引擎 / 固定开发图 | 旧服务 TP / FP / FN | 部署候选 TP / FP / FN |
|---|---:|---:|
| 第一人称16图 |131 / 7 / 83|v8：175 / 13 / 39|
| 第三人称2图 |17 / 2 / 10|v2：23 / 1 / 4|

第一人称是显著减少漏检，同时增加6个误报的取舍，不能称所有类别全面改善。裸手正确检出3→7，手套10→12，烧杯14→15；蓝盖瓶5→4、搅拌子5→4仍有回退。当前固定开发集很小、有历史开发暴露，不能作为独立泛化成绩。

实际视频新旧精扫400帧、粗扫80帧的PTS和解码像素逐一匹配。第一人称精扫中，占图面积超过18%的巨大手/手套框43→0，中央区域烧杯有候选的帧36→160/200。这些是输出覆盖观察，不是逐帧真值准确率。静态第三人称台面接受动作仍为0。两段视频并非同步实验，不能据此宣称跨视角身份或物理动作已验收。

## 真实训练与数据

本轮批量路线完成 **11个检测训练运行**：v1两角色、v2两角色、第一人称v3/v4b/v5b/v6/v7/v8、第三人称v4c。失败的第三人称v4b、AMP探测以及更早手姿/显示窗诊断不混入这个次数。

第一人称v8继承v6检查点、新建优化器，103训练/16固定开发验证，50轮CUDA FP16，实际batch16/尾批7，113.35秒、峰值4,783.05MiB；没有CPU激活转存。最终第50轮检查点在训练前声明，非事后挑选。103图是针对新纠错的回放批次；此前大批训练已经使用4,175张第一人称和8,942张第三人称数据。

第三人称扩大训练v4b在第6轮出现FP16非有限损失；BF16真实监督检查也未通过。有限第5轮权重重新初始化优化器后，v4c以CUDA FP32/batch16完成10轮、1,042.24秒，固定开发效果未胜出，未替换线上v2。数据量增加不自动代表模型进步。

23:19 CST工作台快照：

| 项目 | 数量 | 含义 |
|---|---:|---|
| 冻结有效候选 |18,136图|不含排除的无意义图；新增纠错派生图另记|
| 训练/开发导出累计唯一像素 |13,144图|含伪标签；多轮复用不重复累加|
| 实例分割草稿 |18,129图 / 378,536实例|框提示模型提议，未冒充逐件复核|
| 部分语义草稿 |18,129图 / 110,901区域|仅已标像素，未知区域保持ignore|
| 手姿草稿 |7,294图 / 10,301手|114,670提议点、101,651未知点|
| 本批有实际项目复核的图 |38图|不同任务状态分别保留|

本批实际复核mask为8图/12实例，手姿为6图/6手、4可辨点/122未知点；较早独立手姿项目的144/168可辨点不混入此批。当前不具备把所有分割/手姿草稿视为完整监督训练真值的依据。公开ChemEq25的4,294图保持原本体和未知视角，不强行填第一/第三人称。

## 未参与本轮训练的新素材

另在9月23日新片段固定25%/75%时刻抽取两帧，与旧/v8实际引擎比较。来源未出现在当前批量数据池及v8训练中，但尚未完成全部历史模型接触和近重复审计，因此不称严格独立测试。

一帧原模型把两只手套手合并为大框，v8分为两只；另一帧v8手部置信度仅约0.30/0.27。**两帧中央清晰可见的分析天平均被新旧模型漏检。** 已保留原图、全部输出和问题记录，将据此定向补标训练；一旦用于训练，该素材将明确转为开发用途。

## 尚未解决

- 分割、手部21关键点、跟踪、OCR、动作步骤尚未随本次检测升级一起验收或晋级；完整产品仍未完成。
- 新场景天平、模糊手、相似器材误报及少数旧类回退仍需迭代。
- 14:54:23 UTC旧分析进程被系统内存OOM杀死，随后自动恢复；当时也在构建引擎，因果尚未证明。升级退出又达到180秒超时。两项事故已如实记录。
- 新进程仍出现共享GPU的NMS时限告警；整批有界重试及失败拒绝保留，不能声称长期运行问题已解决。
- 采集、Web、生产分析及自有标注服务当前运行；旧独占GPU暂停窗口已结束，没有延长。只调度自有标注为训练让出GPU。

## 证据与代码

权威NAS根为 `/mnt/realityloop-nas/AnnotationWorkbench/datasets/labprism-source-expansion-20260923-v1/bulk-annotation-v1/`。

- 实际部署与加载：`deployment-first-v8/{receipt,live-verification}.json`、`deployment-third-v2/receipt.json`。
- 修正后的引擎对照：`runtime-acceptance-v8-engine-corrected/fine-engine-report.json`、`runtime-acceptance-third-v2-corrected/fine-engine-report.json`。早期raw-baseline报告保留历史身份，不再用于公平比较结论。
- 完整视频配对：`runtime-video-v8/full-comparison.json`；可视化：`runtime-video-v8/before-after-45.5s.jpg`。
- 新素材：`untrained-source-v8/{input-receipt,comparison,visual-review}.json`。
- 训练权重与原记录：各`training-v*`目录；模型本地，日志/标签/报告NAS。
- 比较修复：`scripts/compare_engine_handoff.py`，回归测试`tests/test_engine_handoff.py`通过；项目检查通过。VisionCortex生产实现已同步两GitHub仓库，AnnotationWorkbench没有配置远端，不能声称已推送。
