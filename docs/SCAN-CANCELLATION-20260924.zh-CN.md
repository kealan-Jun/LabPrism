# 分析故障回收与生产运行时迭代

完整产品目标继续：模型训练、真实比较和合格版本上线持续推进。生产检测当前使用第一人称 v8、第三人称精扫 v5m / 粗扫 v2。扩充手姿仍是检查候选，分割 v6 未超过当前基线，不替换更好的现用能力。

本轮发现分析进程在 2026-09-24 07:09:32 CST 被主机 OOM 终止并自动重启。内核记录匿名 RSS 26,241,132 KiB。先处理会让分析中断的资源问题，以便后续模型阶段能可靠运行。

VisionCortex `6d5c84bfcd8ae1276814b3980622e2cc0161baba` 修复消费者失败后解码线程堵塞、子进程遗留的问题：取消传播到预取、满/空队列和所属解码器，正常完成仍保留帧序和检查点语义。历史运动探测改用同一有界预取；当前生产未启用那条路径，不能把它当作本次 OOM 的直接原因。两个 GitHub 仓库的 main 与共享分支已核对为同一提交。

- 209 项相关测试通过，4 项因 FFmpeg 版本跳过；变更文件 Ruff 和编译检查通过。全仓两个原有采集文件 Ruff 问题保留。
- 同一真实视频 320 帧、同一 v5m TensorRT 模型，前后 JSONL SHA256 均为 `92ad9c37aaf7b9046df8be0ae4f04770de31dbf58b61606add8c3b4cb0f9577a`。输出一致；单次耗时差不代表吞吐提升。
- 通过生产扫描入口注入推理故障：两个真实 FFmpeg 子进程回收，遗留解码线程 0，错误完成标记 0，含准备耗时 1.313 秒。
- 当前部署回执：`/mnt/realityloop-nas/LabPrism/evaluations/scan-cancellation-20260924-v1/runtime-deployment.json`。只切换分析代码，保留旧版本回退；权重、GPU 配额、采集和 Web 不随之修改。

部署后的实际进程、模型加载和扫描证据在同目录 `live-verification.json`。此前故障脚本绕过调度层，未解析 `auto` 策略而失败；该失败保留，随后通过正式调度入口完成测试。NAS 存原始日志、回放和回执，代码本地及 GitHub，未新建训练数据或修改生产方质量保持。

生产进程 2142382 已报告干净提交 `6d5c84b` 并实际加载粗扫 v8 / v2，12 个任务推进，首个验收快照观测到第一人称 322 帧、第三人称 199 帧粗扫。采集 PID 2145、Web PID 1331 未变。旧进程退出仍耗时约 181 秒；新进程启动观测峰值 RSS 24,777,372 KiB，因此没有将“旧进程成功替换”写成“长期内存问题全部解决”。

**仍未证明长期 OOM 全部解决。** 本轮证明的是具体泄漏路径已修复和所测真实视频输出一致。新进程需要继续观察主机内存峰值；源帧索引等其他调用可能仍有开销。本轮不构成模型准确率提升或完整产品验收。接下来继续把已验证的分割、手姿、OCR 输出接入 VisionCortex 实际视频流程，并针对真实错误补监督训练。

只读统计另发现既有回执投影含 2,510 行、共 5,227,577,735 字符，单行最大 569,715,413 字符；最大单日 4 行共 1,229,136,407 字符。现有发布实现会物化整个日集合，仍需限制其内存；这些是存储大小和代码证据，并非堆分析或已证明的全部 OOM 原因。原始统计见同目录 `projection-size-audit.json`。
