# 手姿扩充训练与真实视频 · 2026-09-24

本轮在 AnnotationWorkbench 完成实际扩充训练，LabPrism 接收新推理模型并重算完整152秒视频、接入新旧对照。候选已用于本地检查页；没有宣称生产手姿已替换、握拳问题已解决或独立泛化达标。

## 数据与训练

- 本轮292张唯一有监督图片：287训练、5固定开发验证。训练含275张NAS初标（第一人称99、第三人称176），308只手、4,568个候选关节点；旧有效回放23只手、144已知点。验证6只手、15已知点。
- 这是复用现有素材扩充实际训练覆盖，新增原始媒体0、新增项目复核标签0。保留未知关节点，初标未转为reviewed；任务标签与自动训练质量hold未修改。
- 从既有13,000张伪标签训练候选筛选：11,444与手姿固定验证来源组件相连，831没有手姿初标；剩余725张中450张未通过本轮手框/关节点置信要求。未为了扩数量混入验证来源。168/275初标来自同一采集组，来源仍偏。
- 初标关节点分数≥0.4、至少8点；检测手框分数≥0.5、最短边24像素。未知不补点。初标损失权重0.15，与旧标签回放分开记录；这不是校准过的可见性概率。
- 继承上一版完整RTMPose检查点，使用现有MMPose实现；新AdamW/AMP状态，非完整优化器恢复。全网络训练，BN运行统计保持。256输入、实际batch32/尾22，12轮；每轮331只训练手全部使用，再增加331次旧标签回放，共7,944次呈现，不计为新增图片。
- 与关节点同步旋转0/90/180/270度；已实际查看四向预览。GPU训练/验证16.363秒，峰值1,792.26MiB，0次AMP跳步。第2轮按固定开发指标选为交付权重；后续轮次未更好，不交付最后一轮冒充最佳。
- 权重SHA256：`982a4132081e57cec1c26af442148eee843cd18dee24ddde021326856e277725`。训练检查点、代码本地；输入、标签、日志和报告NAS。

## 同条件结果

| 条件 | 上一版 | 本轮 |
|---|---:|---:|
| 单方向、真实标注手框、开发验证已知点命中 | 14/15 | 14/15 |
| 同条件开发平均归一化误差 | 0.0401301 | 0.0338024 |
| 实际CUDA四方向选择、相同0.3阈值、全部已标点 | 135/159 | 152/159 |
| 上述条件仅训练点 | 122/144 | 139/144 |
| 上述条件仅固定验证点 | 13/15 | 13/15 |
| 戴手套已标点（含训练） | 109/124 | 120/124 |
| 裸手已标点（含训练） | 26/35 | 32/35 |

以上是部分项目标签、真实标注手框条件下的开发对照；不能称端到端检测准确率、独立测试成绩或第三人称质量。不同方向/显示阈值协议的分数分开报告。15个验证点太少，整体提升主要来自训练图。

当前v8同一套检测结果驱动761帧重算：有手姿帧601→642，姿态观察1,063→1,198；所有检测对象、RGB、PTS、时间轴字段逐帧保持。新阶段59.907秒，仅含解码、手姿及适用派生几何，不是全流程速度；未运行跟踪或事件识别，不生成动作结论。

实际查看25/45/65秒：握拳与双手遮挡仍出现错误且高分的完整手指骨架，不能把输出更多等同于正确更多。旧关节未知标签没有提供可见性负监督，继续堆同类初标不能据此宣称问题解决。

## 产品与交付

本地检查页新增“第一人称 · v8 + 扩充训练手姿”，同源上一版可切换并保持时刻。真实浏览器验证了新模型身份、原画/手姿图层、播放/逐帧、对照与390px布局，0页面错误。新快照共12条片段项，保留全部旧版本，348个已验哈希的静态文件使用NAS硬链接复用，避免重复复制；后续更新必须创建新快照，不可就地改共享文件。

VisionCortex分析、采集和Web仍为原PID1223974/2145/1331，未暂停或替换；仅本地预览服务重载到本轮快照。生产检测仍第一人称v8、第三人称精扫v5m/粗扫v2。新视频连续记录、上传、队列与分析由VisionCortex承接，LabPrism旧显示窗作业代码是历史原型；不把它视作现行完整流程，也不在官网重建一套工作台。

本轮实现检查：AW旋转/未知监督/覆盖采样12项，LP手姿交接40项，Ruff及项目检查通过；数据接口的原两次冻结失败保留。冻结时修正了池与工作台像素摘要的不同前缀和libjpeg9.0/6.2解码差异，最终在原池解码器下复核媒体SHA与验证像素身份；不修改历史摘要。

下一步：在VisionCortex既有分析链路核查并整合已验证检测与后续手姿/分割模块；补充握拳/遮挡的明确可见性监督和开发点覆盖。独立泛化、跨机位身份、完整动作/OCR与全天稳定性仍未完成。

权威证据：AW NAS `bulk-annotation-v1/pose-expansion-v2-final`；LP NAS `evaluations/pose-expansion-20260924-v2/acceptance.json`。本轮没有更改FieldRecognition模型或服务。
