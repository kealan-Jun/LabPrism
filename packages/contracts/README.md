# packages/contracts

跨项目版本化契约：训练请求、数据集引用、模型交付与各模块推理结果。`video-result-v1.md` 已接通生产方演示素材回执、推理候选输出和浏览器，校验实现位于 `src/labprism/contracts.py` 与 `artifacts.py`。

正式训练权重的晋级、回归和回滚交付仍待完成；权威标签/schema 的生产与训练实现属于 AnnotationWorkbench。
